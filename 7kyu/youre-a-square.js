let isSquare = function(n) {
  let value = Math.floor(Math.sqrt(n));
  if (value * value == n) return true;
  else {
    return false;
  }
};

function isSquare(n) {
  return Math.sqrt(n) % i === 0;
}
